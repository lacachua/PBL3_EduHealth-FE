import React from 'react';
import {
  normalizeSource,
  SOURCE_LABELS,
  SOURCE_TONE_CLASS_MAP,
} from '../constants/notificationTypes';

const NotificationSourceBadge = () => null;

export default NotificationSourceBadge;
